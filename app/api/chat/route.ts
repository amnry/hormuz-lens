// Streaming chat API route -- LangGraph agent via OpenRouter (Phase 5)
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ error: 'Chat endpoint not yet implemented.' }, { status: 501 });
}
